module.exports = {
    theme: {
      extend: {
        borderColor: {
          border: 'hsl(var(--border))'
        }
      }
    }
  }

// tailwind.config.js
module.exports = {
  darkMode: 'class',
  // rest of your configuration
}